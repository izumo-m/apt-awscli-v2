{
  "name": "apt-awscli-v2-cloudflare",
  "version": "0.0.0",
  "private": true,
  "description": "Worker source and local dev tooling. Production deployment is managed by Pulumi (see ../pulumi/src/cloudflare.ts).",
  "scripts": {
    "dev": "wrangler dev",
    "tail": "wrangler tail apt-awscli-v2-proxy",
    "whoami": "wrangler whoami"
  },
  "devDependencies": {
    "wrangler": "^4.83.0"
  }
}
